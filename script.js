// You can expand this later for real-time funding or contact forms
document.addEventListener('DOMContentLoaded', () => {
  // Example: set progress bar width
  document.querySelector('.progress').style.width = '45%';
});
